plus <- function(a, b)
    return(a + b)

minus <- function(a, b)
    return(a - b)

areaOfCircle <- function(r)
    return(3 * square(r))

areaOfTriangle <- function(bottom, height)
    return(divide(multiply(bottom, height), 2))

# aaaaaaa